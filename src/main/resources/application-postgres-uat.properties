# PostgreSQL UAT – same config as postgres, with UAT overrides.
# Run with: --spring.profiles.active=postgres-uat
spring.config.import=classpath:postgre_pipeline_config.yml

# Override for UAT (replace with your UAT values or use env vars)
pipeline.config.source.jdbc-url=jdbc:postgresql://uat-db-host:5432/${POSTGRES_UAT_DATABASE:marketdb}
pipeline.config.source.username=${POSTGRES_UAT_USERNAME:uat_user}
pipeline.config.source.password=${POSTGRES_UAT_PASSWORD:}
pipeline.config.source.table=${POSTGRES_UAT_TABLE:market_summary}
