# PostgreSQL SIT – same config as postgres, with SIT overrides.
# Run with: --spring.profiles.active=postgres-sit
spring.config.import=classpath:postgre_pipeline_config.yml

# Override for SIT (replace with your SIT values or use env vars)
pipeline.config.source.jdbc-url=jdbc:postgresql://sit-db-host:5432/${POSTGRES_SIT_DATABASE:marketdb}
pipeline.config.source.username=${POSTGRES_SIT_USERNAME:sit_user}
pipeline.config.source.password=${POSTGRES_SIT_PASSWORD:}
pipeline.config.source.table=${POSTGRES_SIT_TABLE:market_summary}
