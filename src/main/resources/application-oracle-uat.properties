# Oracle UAT – same config as oracle, with UAT overrides.
# Run with: --spring.profiles.active=oracle-uat
spring.config.import=classpath:oracle_pipeline_config.yml

# Override for UAT (replace with your UAT values or use env vars)
pipeline.config.source.jdbc-url=jdbc:oracle:thin:@uat-db-host:1521:UAT
pipeline.config.source.username=${ORACLE_UAT_USERNAME:uat_user}
pipeline.config.source.password=${ORACLE_UAT_PASSWORD:}
pipeline.config.source.table=${ORACLE_UAT_TABLE:schema.some_table}
