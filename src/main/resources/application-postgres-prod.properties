# PostgreSQL PROD – same config as postgres, with PROD overrides.
# Run with: --spring.profiles.active=postgres-prod
spring.config.import=classpath:postgre_pipeline_config.yml

# Override for PROD (replace with your PROD values or use env vars)
pipeline.config.source.jdbc-url=jdbc:postgresql://prod-db-host:5432/${POSTGRES_PROD_DATABASE:marketdb}
pipeline.config.source.username=${POSTGRES_PROD_USERNAME:}
pipeline.config.source.password=${POSTGRES_PROD_PASSWORD:}
pipeline.config.source.table=${POSTGRES_PROD_TABLE:market_summary}
