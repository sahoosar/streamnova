# Oracle SIT – same config as oracle, with SIT overrides.
# Run with: --spring.profiles.active=oracle-sit
spring.config.import=classpath:oracle_pipeline_config.yml

# Override for SIT (replace with your SIT values or use env vars)
pipeline.config.source.jdbc-url=jdbc:oracle:thin:@sit-db-host:1521:SIT
pipeline.config.source.username=${ORACLE_SIT_USERNAME:sit_user}
pipeline.config.source.password=${ORACLE_SIT_PASSWORD:}
pipeline.config.source.table=${ORACLE_SIT_TABLE:schema.some_table}
