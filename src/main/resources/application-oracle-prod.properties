# Oracle PROD – same config as oracle, with PROD overrides.
# Run with: --spring.profiles.active=oracle-prod
spring.config.import=classpath:oracle_pipeline_config.yml

# Override for PROD (replace with your PROD values or use env vars)
pipeline.config.source.jdbc-url=jdbc:oracle:thin:@prod-db-host:1521:PROD
pipeline.config.source.username=${ORACLE_PROD_USERNAME:}
pipeline.config.source.password=${ORACLE_PROD_PASSWORD:}
pipeline.config.source.table=${ORACLE_PROD_TABLE:schema.some_table}
